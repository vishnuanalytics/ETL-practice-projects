services:
  - type: web
    name: ETL-practice-projects
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn webhook.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: NEON_DATABASE_URL
        sync: false